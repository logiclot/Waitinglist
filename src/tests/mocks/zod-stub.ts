// Stub for zod - used when the package is not installed in test environment
const createSchema = () => {
  const schema: Record<string, unknown> = {
    parse: (val: unknown) => val,
    safeParse: (val: unknown) => ({ success: true, data: val }),
    optional: () => schema,
    nullable: () => schema,
    default: () => schema,
    transform: () => schema,
    refine: () => schema,
    pipe: () => schema,
    array: () => schema,
    min: () => schema,
    max: () => schema,
    email: () => schema,
    url: () => schema,
    uuid: () => schema,
    trim: () => schema,
    length: () => schema,
    regex: () => schema,
    startsWith: () => schema,
    endsWith: () => schema,
    nonempty: () => schema,
    positive: () => schema,
    negative: () => schema,
    int: () => schema,
    finite: () => schema,
    nonnegative: () => schema,
    nonpositive: () => schema,
    gte: () => schema,
    lte: () => schema,
    gt: () => schema,
    lt: () => schema,
    extend: () => schema,
    merge: () => schema,
    pick: () => schema,
    omit: () => schema,
    partial: () => schema,
    shape: {},
    enum: () => schema,
    describe: () => schema,
  };
  return schema;
};

export const z = {
  string: () => createSchema(),
  number: () => createSchema(),
  boolean: () => createSchema(),
  object: () => createSchema(),
  array: () => createSchema(),
  enum: () => createSchema(),
  union: () => createSchema(),
  literal: () => createSchema(),
  any: () => createSchema(),
  unknown: () => createSchema(),
  void: () => createSchema(),
  null: () => createSchema(),
  undefined: () => createSchema(),
  optional: () => createSchema(),
  nullable: () => createSchema(),
  record: () => createSchema(),
  tuple: () => createSchema(),
  intersection: () => createSchema(),
  lazy: () => createSchema(),
  custom: () => createSchema(),
  coerce: {
    string: () => createSchema(),
    number: () => createSchema(),
    boolean: () => createSchema(),
  },
  nativeEnum: () => createSchema(),
  ZodError: class ZodError extends Error {
    issues: unknown[] = [];
  },
  infer: undefined,
};

export default z;
